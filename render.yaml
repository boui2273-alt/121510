services:
  - type: web
    name: school-activity-dashboard
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT school_web_dashboard:app
    healthCheckPath: /api/schools
    autoDeploy: true